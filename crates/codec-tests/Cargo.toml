[package]
name = "strata-codec-tests"
version = "0.1.0"
edition = "2024"
description = "Property testing macros and utilities for strata-codec"

[dependencies]
strata-codec = { path = "../codec" }

paste = "1.0"
proptest = "1.4"

[lints]
workspace = true
